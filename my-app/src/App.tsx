import React, { ReactElement } from 'react';
import './App.css';

const App: React.FC = (): ReactElement<HTMLElement> => {
    return (
        <div className="App">
            Pomodoro Timer
        </div>
    );
}

export default App;
